# Legacy file — use pyproject.toml for modern installs.
# pip install .          # basic
# pip install .[dev]     # with dev deps
# pip install .[web]     # with web UI
# pip install .[multimodal]  # with multimodal support
